#Application Configuration
server:
  port: 8003

#Monodb Configuration
spring:
  data:
    mongodb:
      database: empty_db
      host: localhost
      port: 27017

#logging
logging:
  level:
    org.springframework.data.mongodb: DEBUG
